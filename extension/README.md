# Dorvey Browser Extension

Chrome extension for quick access to Dorvey API.

1. Load unpacked: Chrome → Extensions → Load unpacked → select `extension` folder
2. Add icon48.png (48x48) to the folder, or extension will use default
3. Click extension icon → enter JWT token and API URL → Save
4. Token can be copied from Dorvey Settings or after login
